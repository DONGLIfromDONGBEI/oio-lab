/**
 * 首页区块开关：正式发布 / 重新开放招募时改为 true 即可显示，无需删组件。
 * 文案以 docs/COURSE_INFO_AND_COUNTDOWN.md 为备忘，同步到 lib/dictionaries.ts（及 Countdown 截止时间）。
 *
 * 本期招生已结束：`SHOW_COURSE_INFO_CARD` 暂为 false；下期有确定课表后再开。
 */
export const SHOW_COURSE_INFO_CARD = false;
/** 距本批招募截止：本期关闭；下期改 true 即恢复 */
export const SHOW_COUNTDOWN_SECTION = true;
